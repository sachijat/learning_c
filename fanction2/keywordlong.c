#include<stdio.h>

int main()
{
   int a;
   long b ;
   long long c;
   double e;
   long double f;
   printf(" the size of int =%dbytes\n",sizeof(a));
   printf("the size of long =%d bytes\n",sizeof(b));
   printf("the size of long long =%d bytes\n ",sizeof(c));
printf("the size of double =%d bytes\n",sizeof(e));
printf("the size of double long=%d bytes\n",sizeof(f));
 return 0;


     
}